import { NodeDefinition } from "./NodeDefinition";

export class NodeDefinitionParser {
    static ParseNodeDefintion(dv: DataView): NodeDefinition {
        const nd = new NodeDefinition()
        //TODO
        return nd
    }
}